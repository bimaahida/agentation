// Injected by esbuild so the classic JSX transform (React.createElement) has
// React in scope. Classic transform is used instead of the automatic one
// because `react/jsx-runtime` only exists in React >= 16.14.
import * as React from "react";
export { React };
