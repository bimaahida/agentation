# npm prefers this over .gitignore when packing, and .gitignore excludes dist/.
# dist must ship: it is the published output, and this fork is also consumed by
# git tag (see scripts/release-tag.sh), where the tag's tree carries the build.
example/
